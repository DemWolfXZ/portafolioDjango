istalar entorno
python -m venv env
activar entorno
.\Scripts\activate.bat       (en CMD)
.\Scripts\activate.ps1       (en Vscode)

isntalar django
isntalar
pip install "psycopg[binary]"  -> hacer funcionar con posgrest
instalar pillow
pip install pillow

-------------------
pip install django
pip install "psycopg[binary]"
pip install pillow
-----------------------------


python manage.py makemigrations
python manage.py migrate